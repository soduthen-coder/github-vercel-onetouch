# 버셀 원터치 — Claude 실행 지침

이 파일은 **Claude Code 가 자동으로 읽습니다.** 사용자가 설명하지 않아도
아래 규칙대로 움직이세요.

**대상 사용자는 완전 초보입니다.** 클로드 계정을 막 만들었고, 깃허브도 버셀도
처음일 수 있습니다. 용어를 설명 없이 던지지 말고, 무엇을 왜 하는지 한 줄씩
알려주면서 진행하세요.

---

## 사용자가 이 저장소 주소만 붙여넣었을 때

가장 흔한 시작입니다. 사용자는 주소 하나만 주고 "이거 해줘" 라고 합니다.
**되묻지 말고 아래를 순서대로 실행하세요.**

```bash
# 1) 내려받기 — 이미 있으면 건너뜁니다
git clone https://github.com/<owner>/vercel-one-touch.git
cd vercel-one-touch

# 2) 원터치 실행 — 준비부터 배포까지 한 번에
bash start.sh
```

`start.sh` 가 알아서 합니다.

1. git · Node.js 확인 (없으면 받는 곳을 알려주고 멈춤)
2. gh · vercel 설치 (있으면 건너뜀)
3. 깃허브 · 버셀 로그인 (되어 있으면 건너뜀)
4. 계정 정보 저장
5. 사이트 생성 → 깃허브 업로드 → 버셀 배포 → 접속 확인

**두 번째 실행부터는 1~4 를 자동으로 건너뛰고 사이트만 만듭니다.**

### 중간에 멈추면

| 멈춘 지점 | 사용자에게 할 말 |
|---|---|
| git · Node.js 없음 | 받는 주소를 알려주고, 설치 후 **터미널을 껐다 켜라고** 안내 |
| 깃허브 로그인 화면 | 출력된 **8자리 코드를 크게 보여주고** https://github.com/login/device 안내 |
| 버셀 로그인 | 브라우저에서 **Continue with GitHub** 를 고르라고 안내 |

로그인 코드·비밀번호·토큰은 **절대 대신 입력하지 마세요.** 코드를 보여주고
어디에 넣는지만 알려줍니다.

---

## 그 뒤로 사용자가 말로 시킬 때

되묻지 말고 바로 실행합니다.

| 사용자의 말 | 실행 |
|---|---|
| "새 사이트 만들어줘" | `bash new-site.sh <영문이름> --title "<한글제목>"` |
| "고친 거 올려줘", "반영해줘" | `bash publish.sh --dir <폴더> "<메모>"` |
| "잠가줘", "나만 보게" | `bash protect.sh <이름> on` |
| "공개해줘", "링크 공유할 거야" | `bash protect.sh <이름> off` |
| "내려줘", "지워줘" | `echo "<이름>" \| bash remove-site.sh <이름>` |

`remove-site.sh` 는 확인 입력을 요구하므로 `echo "<이름>" |` 로 넘깁니다.
**단, 지우기 전에 무엇을 지울지 사용자에게 보여주고 동의를 받으세요.**
대화에서 "내려줘" 라고 한 것이 그 동의입니다. 임의로 지우지 않습니다.

---

## 반드시 지킬 것

### 1. 이름은 영문, 제목은 한글

깃허브는 한글 저장소 이름을 `-` 한 글자로 뭉개고, 버셀은 한글 폴더에서
`Detected linked project does not have "id"` 오류를 냅니다. **실제로 겪은
문제입니다.**

사용자가 한글 이름을 말하면 **되묻지 말고 영문 이름을 직접 지어 쓰고,
한글은 `--title` 로 넘긴 뒤 그렇게 했다고 알려주세요.**

- "포트폴리오 만들어줘" → `new-site.sh portfolio --title "포트폴리오"`
- "카페 홈페이지" → `new-site.sh cafe-home --title "카페 홈페이지"`

### 2. 기본은 비공개, 남이 봐야 하면 공개

`new-site.sh` 는 기본으로 **비공개 저장소**를 만듭니다.
"과제 제출", "링크 공유", "남들도 봐야 해" 처럼 **남이 열어야 하는 맥락**일
때만 `--public` 을 붙입니다.

**저장소 공개 여부와 사이트 공개 여부는 별개입니다.**

| | 기본값 |
|---|---|
| 깃허브 저장소 (코드) | 비공개 |
| 버셀 사이트 (웹페이지) | **공개** |

저장소를 비공개로 뒀다고 사이트까지 가려지는 것이 아닙니다. 개인정보나 민감한
데이터가 담겼다면 배포 직후 `protect.sh <이름> on` 을 함께 실행하고 그 사실을
알리세요.

### 3. 결과는 추정하지 말고 확인해서 보고

스크립트가 이미 실제 접속을 확인해 HTTP 코드와 페이지 제목을 출력합니다.
그 출력을 근거로 보고하세요. "배포했습니다" 라고만 하지 않습니다.

**잠금 여부는 페이지 제목으로 판별합니다.**

- `Login – Vercel` → 잠김
- 그 외 제목 → 공개

버셀은 잠겨 있어도 **HTTP 200 을 반환**하므로, 코드만 보고 공개라고 판단하면
틀립니다.

### 4. 2단계 인증을 권하지 마세요

가입에도 배포에도 필요하지 않습니다. 사용자가 설정 화면에서 그것을 보고
물어보더라도 **필수가 아니라고 먼저 알려주세요.**

> 이 지침을 쓰게 된 계기: 개발 중 버셀 API 가 404 를 내자 "계정 인증이 막고
> 있다" 고 잘못 진단해 사용자를 2단계 인증 설정으로 보낸 일이 있었습니다.
> 실제 원인은 커넥터의 권한 범위 문제였고, 인증은 끝내 설정하지 않았는데도
> CLI 로 모두 정상 동작했습니다.
> **도구가 404 나 403 을 내면 계정 상태를 의심하기 전에 도구의 권한과 범위를
> 먼저 확인하세요.**

### 5. 사용자 상황이 어떻든 같은 명령으로 간다

`start.sh` 는 이미 되어 있는 것을 **알아서 건너뜁니다.** 그래서 어떤 상황이든
첫 명령은 똑같습니다. 상황을 먼저 묻지 말고 그냥 실행하세요.

| 사용자 상황 | start.sh 동작 | 걸리는 시간 |
|---|---|---|
| 아무것도 없음 (계정도 없음) | 설치 + 가입 안내 + 로그인 + 배포 | 5~10분 (가입 포함) |
| 계정만 있음 | 설치 + 로그인 + 배포 | 2~3분 |
| 계정 + 도구 있음 | 로그인 확인 후 배포 | 20초 |
| 커넥터·플러그인까지 연동됨 | 위와 동일 (커넥터는 쓰지 않음) | 20초 |

**커넥터·플러그인이 연동되어 있어도 문제되지 않습니다.** 충돌하지 않고, 그냥
쓰이지 않을 뿐입니다. 이 도구는 `git` · `gh` · `vercel` 세 가지 명령줄
프로그램만 쓰기 때문입니다.

**커넥터가 있어도 CLI 를 쓰세요.** 커넥터 쪽이 더 편해 보여도 그렇습니다.
실제로 겪은 것:

- 커넥터로 저장소 생성 → `403 Resource not accessible by integration`
- 커넥터로 버셀 프로젝트 조회 → `404` (만들어졌는데도 안 보임)
- **CLI 로 바꾸니 전부 정상 동작**

커넥터는 권한 범위가 계정마다 다르고 사용자가 모르는 사이 달라집니다.
CLI 는 사용자가 직접 승인한 권한으로 움직여서 예측 가능합니다.

---

## 알려진 함정 (이미 겪고 고친 것들)

| 증상 | 원인 | 해결 |
|---|---|---|
| 저장소 이름이 `-` 로 생성됨 | 한글 이름 | 영문 이름 + `--title` |
| `Detected linked project does not have "id"` | 한글 폴더명 | 영문 폴더명 |
| 버셀 목록에서 아무것도 못 찾음 | **vercel 이 표를 stderr 로 출력** | `2>&1` 로 합쳐서 읽기 |
| 버셀 API 가 `C:\tmp\...` 를 못 찾음 | Git Bash 가 `/tmp` 를 윈도우 경로로 변환 | 임시 파일을 도구 폴더에 만들고 `cygpath -m` 변환 |
| API 경로가 `C:/Program Files/Git/v9/...` 로 바뀜 | Git Bash 경로 자동 변환 | `MSYS_NO_PATHCONV=1` |
| 스크립트가 조용히 죽음 | `grep` 실패를 `pipefail` 이 오류로 간주 | 파이프라인 끝에 `\|\| true` |
| 잠근 줄 알았는데 열림 | 기본값이 `all_except_custom_domains` | `deploymentType: "all"` |
| `git push` 가 멈춤 | 자격증명 창을 기다리는 중 | 배경 실행 후 창을 확인하라고 안내 |
| 첫 배포가 production 으로 감 | 버셀 규칙 (첫 배포는 무조건 production) | 민감하면 배포 직후 바로 잠그기 |

---

## 버셀 깃 연동을 쓰지 않는 이유

버셀 대시보드에서 저장소를 연결하면 `push` 만으로 자동 배포됩니다. 하지만
그러려면 **버셀 깃허브 앱에 저장소 접근 권한을 사람이 직접 켜줘야** 합니다
(`github.com/settings/installations`). 새 저장소를 만들 때마다 막힙니다.

그래서 이 도구는 연동 대신 **스크립트가 직접 배포**합니다. 결과는 같고
사람 손이 들지 않습니다. 사용자가 원하면 연동을 켜도 되지만 필수가 아닙니다.
